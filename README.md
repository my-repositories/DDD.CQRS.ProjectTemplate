# DDD.CQRS.ProjectTemplate
Template to use when creating a .NET Core application.

## Installation

```
dotnet new --install DDD.CQRS.ProjectTemplate::1.0.0
```

## Usage

```
dotnet new ddd.cqrs --includeTest --projectName=Cool.ProjectX
```